# Configuration can be changed from the dashboard only after authentication is added.
# The runtime environment remains the safe default source until that control layer is enabled.

mode: draft_only
auto_publish: false
daily_publish_limit: 25
ratings:
  5: approval
  4: approval
  3: approval
  2: escalation
  1: escalation
risk:
  low: approval
  medium: approval
  high: escalation
